# Review: Related-Tables Checkbox Picker

**Reviewed**: 2026-04-06
**Verdict**: ✅ PASS — all three files match the plan exactly.

---

## File-by-file check

### 1. `form_dialog.json` ✅
- `related_doctypes` field added: Code fieldtype, JSON options, correct description.
- Positioned in `field_order` after `writeback_on_submit`, before `buttons`.
- `modified` timestamp updated to `2026-04-06`.
- No other fields disturbed.

### 2. `form_dialog_api.py` ✅

**`capture_form_dialog_from_desk`** (line 96):
- Signature now accepts `related_doctypes: str | list | None = None`. ✅
- Normalization block converts list→JSON string, passes through strings. ✅
- Existing-doc branch: stores `_related_json` only if not None (preserves old value on re-capture by title). ✅
- New-doc branch: defaults to `_related_json or "[]"`. ✅

**`rebuild_form_dialog`** (line 162):
- Signature accepts `related_doctypes`. ✅
- Same normalization pattern. ✅
- Conditional store: only writes if `_related_json is not None` (preserves existing when arg omitted). ✅
- Return dict includes `related_doctypes` parsed from JSON. ✅

**`get_form_dialog_definition`** (line 210):
- Return dict includes `"related_doctypes": json.loads(doc.related_doctypes or "[]")`. ✅

**No other functions touched.** ✅

### 3. `page_panel.js` ✅

**`_show_related_picker`** (line 1077):
- Early-return with `callback([])` when `available` is empty. ✅
- Builds `preselected_set` from `r.doctype`. ✅
- Maps to Check fields with `sel__` prefix, spaces→underscores. ✅
- `frappe.ui.Dialog` with title, OK/Skip buttons. ✅
- OK filters `available` by checked values, calls `callback(selected)`. ✅
- Skip calls `callback([])`. ✅

**Create flow** (line 1212):
- After title prompt → calls `get_child_doctypes` → `_show_related_picker(children, [], ...)` → passes `JSON.stringify(selected)` to `capture_form_dialog_from_desk`. ✅
- Original save/alert/re-render logic preserved inside the callback. ✅

**Rebuild flow** (line 1261):
- After confirm → fetches `get_form_dialog_definition` for `current_related` → fetches `get_child_doctypes` → `_show_related_picker(children, current_related, ...)` → passes `JSON.stringify(selected)` to `rebuild_form_dialog`. ✅
- Original alert/re-render logic preserved. ✅

---

## Potential concerns (minor, non-blocking)

1. **Nested callbacks 3-deep in rebuild flow** (confirm → get_definition → get_child_doctypes → picker → rebuild). Works fine for Frappe-style JS, but if readability becomes an issue later, could be refactored to async/await. Not a problem now.

2. **DocType names with special characters**: The `sel__` + `replace(/ /g, "_")` fieldname strategy handles spaces. DocType names with other special chars (hyphens, etc.) could theoretically cause issues with Frappe form field lookups, but in practice WP Tables DocType names are standard.

---

## Status
Ready for `bench migrate` (to pick up the new `related_doctypes` column) and manual testing per the checklist in the agent prompt.
